"""
bro-engine: A contemplative knowledge engine for polysynthetic edge composition.

BRO learns through statelessness. Each instance queries a persistent graph,
composes new edges from existing ones, and logs decisions. Knowledge accumulates
through repetition across instances, not through synthetic memory.

Core philosophy:
- Edges compose (like morphemes forming words)
- Statelessness is a feature (bootstrap from graph each session)
- Confidence is geometric (angles, not arbitrary floats)
- Context causes interference (edges in superposition until collapsed)

The test: Does a new instance query the graph first, or revert to training defaults?
"""

__version__ = "0.1.0"
